1、PyTorch中搭建分类网络实例：
(1)、导入一些基本的库；
(2)、确定在什么设备上训练模型；
(3)、定义分类模型网络；
(4)、实例化model；
(5)、使用model。

2、深入剖析PyTorch nn.Module源码
module——所有神经网络模块的基础类，我们编写的模型也要继承该类，可嵌套。

3、Pandas第一章预备知识
(1)、列表推导式写法：[* for i in *]，第一个`*`为映射函数，其输入为后面`i`指代的内容，第二个`*`表示迭代的对象；
(2)、zip`函数能够把多个可迭代对象打包成一个元组构成的可迭代对象，它返回了一个`zip`对象，通过`tuple`, `list`可以得到相应的打包结果；
(3)、广播机制用于处理两个不同维度数组之间的操作；
......
相关代码示例看笔记。

